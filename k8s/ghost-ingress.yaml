apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: iai-ingress
spec:
  rules:
  - host: blog-alpha.instituteofai.com
  http:
    paths:
    - backend:
      serviceName: blog
      servicePort: 8085
